# 📦 Taqnia Distribution Manager - التوثيق النهائي الشامل

## 🎯 حالة المشروع: ✅ مكتمل 100%

**تاريخ التوثيق:** 2024  
**الإصدار:** 1.0.0  
**الحالة:** جاهز للإنتاج

---

## 📋 ملخص تنفيذي

تم تطوير نظام Taqnia Distribution Manager بنجاح كنظام متكامل لإدارة سلسلة التوزيع والديون من المصنع إلى المتاجر. النظام مبني على Laravel 10 مع RESTful API ويتضمن 8 عمليات رئيسية مع توثيق كامل لكل حركة بضاعة أو مال.

---

## ✅ العمليات المكتملة (8/8)

### 1️⃣ طلب بضاعة من المسوق ✅

**الوصف:** المسوق يطلب بضاعة من المخزن الرئيسي  
**المراحل:** pending → approved → documented  
**الجداول:** marketer_requests, marketer_request_items, main_stock, marketer_reserved_stock, marketer_actual_stock

**API Endpoints:**
```
POST   /api/marketer/requests              - إنشاء طلب
GET    /api/marketer/requests              - قائمة الطلبات
GET    /api/marketer/requests/{id}         - تفاصيل طلب
PUT    /api/marketer/requests/{id}/cancel  - إلغاء طلب

PUT    /api/warehouse/requests/{id}/approve  - موافقة
PUT    /api/warehouse/requests/{id}/reject   - رفض
POST   /api/warehouse/requests/{id}/document - توثيق
```

**Controllers:**
- `Api\Marketer\MarketerRequestController`
- `Api\Warehouse\WarehouseRequestController`

**Models:** MarketerRequest, MarketerRequestItem

---

### 2️⃣ إرجاع بضاعة من المسوق ✅

**الوصف:** المسوق يرجع بضاعة إلى المخزن الرئيسي  
**المراحل:** pending → approved → documented  
**الجداول:** marketer_return_requests, marketer_return_items, marketer_actual_stock, main_stock

**API Endpoints:**
```
POST   /api/marketer/returns              - إنشاء إرجاع
GET    /api/marketer/returns              - قائمة الإرجاعات
GET    /api/marketer/returns/{id}         - تفاصيل إرجاع
PUT    /api/marketer/returns/{id}/cancel  - إلغاء إرجاع

PUT    /api/warehouse/returns/{id}/approve  - موافقة
PUT    /api/warehouse/returns/{id}/reject   - رفض
POST   /api/warehouse/returns/{id}/document - توثيق
```

**Controllers:**
- `Api\Marketer\MarketerReturnController`
- `Api\Warehouse\WarehouseReturnController`

**Models:** MarketerReturnRequest, MarketerReturnItem

---

### 3️⃣ بيع بضاعة للمتجر ✅

**الوصف:** المسوق يبيع بضاعة للمتجر مع تطبيق العروض والخصومات  
**المراحل:** pending → approved / cancelled  
**الجداول:** sales_invoices, sales_invoice_items, marketer_actual_stock, store_pending_stock, store_actual_stock, store_debt_ledger

**API Endpoints:**
```
POST   /api/marketer/sales              - إنشاء فاتورة بيع
GET    /api/marketer/sales              - قائمة الفواتير
GET    /api/marketer/sales/{id}         - تفاصيل فاتورة
PUT    /api/marketer/sales/{id}/cancel  - إلغاء فاتورة
GET    /api/marketer/sales/{id}/rejection - معلومات الرفض

POST   /api/warehouse/sales/{id}/approve - توثيق
PUT    /api/warehouse/sales/{id}/reject  - رفض
```

**Controllers:**
- `Api\Marketer\MarketerSalesController`
- `Api\Warehouse\WarehouseSalesController`

**Models:** SalesInvoice, SalesInvoiceItem, SalesInvoiceRejection

---

### 4️⃣ إيصال القبض (تسديد دين المتجر) ✅

**الوصف:** المتجر يسدد دينه وتسجل عمولة للمسوق  
**المراحل:** pending → approved / rejected  
**الجداول:** store_payments, store_debt_ledger, marketer_commissions

**API Endpoints:**
```
POST   /api/marketer/payments              - إنشاء إيصال قبض
GET    /api/marketer/payments              - قائمة الإيصالات
GET    /api/marketer/payments/{id}         - تفاصيل إيصال
PUT    /api/marketer/payments/{id}/cancel  - إلغاء إيصال

POST   /api/warehouse/payments/{id}/approve - موافقة وتوثيق
PUT    /api/warehouse/payments/{id}/reject  - رفض
```

**Controllers:**
- `Api\Marketer\MarketerPaymentController`
- `Api\Warehouse\WarehousePaymentController`

**Models:** StorePayment, MarketerCommission, StoreDebtLedger

---

### 5️⃣ سحب أرباح المسوق ✅

**الوصف:** المسوق يسحب عمولاته المتراكمة  
**المراحل:** pending → approved / rejected  
**الجداول:** marketer_withdrawal_requests, marketer_commissions

**API Endpoints:**
```
POST   /api/marketer/withdrawals              - طلب سحب
GET    /api/marketer/withdrawals              - قائمة السحوبات
GET    /api/marketer/withdrawals/balance      - الرصيد المتاح
GET    /api/marketer/withdrawals/{id}         - تفاصيل سحب
PUT    /api/marketer/withdrawals/{id}/cancel  - إلغاء طلب

POST   /api/admin/withdrawals/{id}/approve    - موافقة وصرف
POST   /api/admin/withdrawals/{id}/reject     - رفض
```

**Controllers:**
- `Api\Marketer\MarketerWithdrawalController`
- `Api\Admin\AdminWithdrawalController`

**Models:** MarketerWithdrawalRequest

---

### 6️⃣ إرجاع بضاعة من المتجر ✅

**الوصف:** المتجر يرجع بضاعة للمسوق وتقليل الدين  
**المراحل:** pending → approved / rejected  
**الجداول:** sales_returns, sales_return_items, store_actual_stock, store_return_pending_stock, marketer_actual_stock, store_debt_ledger

**API Endpoints:**
```
POST   /api/marketer/store-returns              - إنشاء إرجاع
GET    /api/marketer/store-returns              - قائمة الإرجاعات
GET    /api/marketer/store-returns/{id}         - تفاصيل إرجاع
PUT    /api/marketer/store-returns/{id}/cancel  - إلغاء إرجاع

POST   /api/warehouse/store-returns/{id}/approve - موافقة وتوثيق
PUT    /api/warehouse/store-returns/{id}/reject  - رفض
```

**Controllers:**
- `Api\Marketer\MarketerStoreReturnController`
- `Api\Warehouse\WarehouseStoreReturnController`

**Models:** SalesReturn, SalesReturnItem

---

### 7️⃣ إدارة خصومات الفواتير ✅

**الوصف:** نظام خصومات تلقائية على الفواتير حسب القيمة  
**الأنواع:** نسبة مئوية أو مبلغ ثابت  
**الجداول:** invoice_discount_tiers

**API Endpoints:**
```
GET    /api/admin/discounts              - قائمة الخصومات
POST   /api/admin/discounts              - إنشاء خصم
GET    /api/admin/discounts/{id}         - تفاصيل خصم
PUT    /api/admin/discounts/{id}/toggle  - تفعيل/تعطيل
DELETE /api/admin/discounts/{id}         - حذف (soft delete)

GET    /api/discounts/active             - الخصومات النشطة (للجميع)
```

**Controllers:**
- `Api\Admin\InvoiceDiscountController`

**Models:** InvoiceDiscountTier

---

### 8️⃣ إدارة العروض الترويجية ✅

**الوصف:** نظام "اشتري X واحصل على Y مجاناً"  
**التطبيق:** تلقائي عند إنشاء فاتورة البيع  
**الجداول:** product_promotions

**API Endpoints:**
```
GET    /api/admin/promotions              - قائمة العروض
POST   /api/admin/promotions              - إنشاء عرض
GET    /api/admin/promotions/{id}         - تفاصيل عرض
PUT    /api/admin/promotions/{id}/toggle  - تفعيل/تعطيل
DELETE /api/admin/promotions/{id}         - حذف (soft delete)

GET    /api/marketer/promotions/active    - العروض النشطة
```

**Controllers:**
- `Api\Admin\ProductPromotionController`
- `Api\Marketer\PromotionController`

**Models:** ProductPromotion

---

## 🗄️ قاعدة البيانات

### الجداول الأساسية (4)
1. `roles` - الأدوار
2. `users` - المستخدمون
3. `products` - المنتجات
4. `stores` - المتاجر

### جداول المخزون (7)
5. `main_stock` - المخزن الرئيسي
6. `marketer_reserved_stock` - مخزون المسوق المحجوز
7. `marketer_actual_stock` - مخزون المسوق الفعلي
8. `store_pending_stock` - مخزون المتجر المرحلي
9. `store_actual_stock` - مخزون المتجر الفعلي
10. `store_return_pending_stock` - مخزون الإرجاع المرحلي
11. `warehouse_stock_logs` - سجل حركات المخزن

### جداول العمليات (14)
12. `marketer_requests` - طلبات المسوق
13. `marketer_request_items` - بنود طلبات المسوق
14. `marketer_return_requests` - إرجاعات المسوق
15. `marketer_return_items` - بنود إرجاعات المسوق
16. `sales_invoices` - فواتير البيع
17. `sales_invoice_items` - بنود فواتير البيع
18. `sales_invoice_rejections` - رفض فواتير البيع
19. `sales_returns` - إرجاعات المتاجر
20. `sales_return_items` - بنود إرجاعات المتاجر
21. `store_payments` - إيصالات القبض
22. `store_debt_ledger` - دفتر ديون المتاجر
23. `marketer_commissions` - عمولات المسوقين
24. `marketer_withdrawal_requests` - طلبات سحب الأرباح
25. `product_promotions` - العروض الترويجية
26. `invoice_discount_tiers` - خصومات الفواتير

**إجمالي الجداول: 26 جدول**

---

## 🏗️ البنية التقنية

### Backend
- **Framework:** Laravel 10
- **PHP Version:** 8.1+
- **Database:** MySQL 5.7+
- **Authentication:** Laravel Sanctum
- **Architecture:** RESTful API

### Models (25 Model)
✅ جميع الـ Models موجودة ومطابقة للجداول

### Migrations (27 Migration)
✅ جميع الـ Migrations موجودة ومرتبة بشكل صحيح

### Controllers (19 API Controller)
- **Marketer Controllers:** 9
- **Warehouse Controllers:** 5
- **Admin Controllers:** 5

### Routes Structure
```
routes/
├── api.php              # Main router
├── api/
│   ├── auth.php         # Authentication routes
│   ├── admin.php        # Admin routes
│   ├── marketer.php     # Marketer routes
│   ├── warehouse.php    # Warehouse routes
│   └── common.php       # Shared routes
```

---

## 🔐 الأدوار والصلاحيات

### 1. Admin (المدير)
- إدارة المنتجات والمتاجر
- إدارة المستخدمين
- إدارة العروض الترويجية
- إدارة خصومات الفواتير
- الموافقة على سحب الأرباح
- عرض جميع التقارير

### 2. Warehouse Keeper (أمين المخزن)
- الموافقة/رفض طلبات المسوقين
- توثيق استلام/إرجاع البضاعة
- توثيق فواتير البيع
- الموافقة على إيصالات القبض
- الموافقة على إرجاعات المتاجر

### 3. Salesman/Marketer (المسوق)
- طلب بضاعة من المخزن
- إرجاع بضاعة للمخزن
- بيع بضاعة للمتاجر
- إنشاء إيصالات قبض
- إنشاء إرجاعات من المتاجر
- طلب سحب الأرباح
- عرض المخزون والعمولات

---

## 📡 RESTful API Structure

### Authentication
```
POST   /api/auth/login
POST   /api/auth/logout
GET    /api/auth/user
```

### HTTP Methods
- **GET** - استرجاع البيانات
- **POST** - إنشاء جديد
- **PUT** - تحديث موجود
- **DELETE** - حذف (soft delete)

### Response Format
```json
{
  "message": "رسالة توضيحية",
  "data": { ... }
}
```

### Status Codes
- **200** - نجاح
- **201** - تم الإنشاء
- **400** - خطأ في البيانات
- **404** - غير موجود
- **500** - خطأ في الخادم

---

## 🔄 دورة حياة العمليات

### مثال: طلب بضاعة من المسوق
```
1. pending     → المسوق ينشئ الطلب (لا يتأثر المخزون)
2. approved    → أمين المخزن يوافق (حجز البضاعة)
3. documented  → توثيق الاستلام (نقل للمخزون الفعلي)
```

### مثال: بيع بضاعة للمتجر
```
1. pending   → المسوق ينشئ الفاتورة (نقل للمخزون المرحلي)
2. approved  → أمين المخزن يوثق (نقل للمخزون الفعلي + تسجيل الدين)
```

---

## 💰 نظام الديون والعمولات

### حساب دين المتجر
```
الدين الكلي = مجموع (sale) - مجموع (return) - مجموع (payment)
```

### حساب عمولة المسوق
```
العمولة = المبلغ المسدد × نسبة العمولة
الرصيد المتاح = مجموع العمولات - مجموع السحوبات
```

### ملاحظات مهمة
- ✅ العمولات تُسجل عند التسديد فقط (ليس عند البيع)
- ✅ الإرجاعات تقلل الدين ولا تؤثر على العمولات
- ✅ السحب يتطلب موافقة الإدارة

---

## 🎁 نظام العروض والخصومات

### العروض الترويجية
- **النوع:** اشتري X واحصل على Y مجاناً
- **التطبيق:** تلقائي عند إنشاء فاتورة البيع
- **التأثير:** الكمية المجانية تخصم من المخزون ولا تحسب في السعر

### خصومات الفواتير
- **الأنواع:** نسبة مئوية أو مبلغ ثابت
- **التطبيق:** تلقائي حسب قيمة الفاتورة
- **التأثير:** تخصم من المبلغ النهائي

### ترتيب التطبيق
```
1. حساب subtotal (الكمية × السعر)
2. تطبيق العروض الترويجية (كمية مجانية)
3. حساب product_discount (قيمة الهدايا)
4. تطبيق invoice_discount (خصم الفاتورة)
5. حساب total_amount (المبلغ النهائي)
```

---

## ⚠️ قواعد العمل المهمة

1. ✅ كل حركة بضاعة أو مال يجب أن تكون موثقة
2. ✅ استخدام Transactions لجميع عمليات المخزون
3. ✅ لا تعديل العروض/الخصومات بعد الإنشاء
4. ✅ الحسابات الديناميكية أفضل من الحقول الثابتة
5. ✅ السعر المسجل = السعر وقت البيع
6. ✅ العمولات تتأثر بالتسديد فقط (ليس بالإرجاع)
7. ✅ البضاعة تُحجز عند الموافقة (ليس عند الإنشاء)
8. ✅ الدين يُسجل عند التوثيق (ليس عند الإنشاء)

---

## 📊 التقارير المتاحة

### تقارير المخزون
- المخزن الرئيسي
- مخزون المسوقين (محجوز + فعلي)
- مخزون المتاجر (مرحلي + فعلي)
- حركة المخزون (warehouse_stock_logs)

### تقارير المبيعات
- فواتير البيع حسب الحالة
- مبيعات المسوق
- مبيعات المتجر

### تقارير الديون
- ديون المتاجر
- إيصالات القبض
- الإرجاعات

### تقارير العمولات
- عمولات المسوقين
- سحوبات الأرباح
- الرصيد المتاح

---

## 🚀 التثبيت والإعداد

### المتطلبات
- PHP 8.1+
- MySQL 5.7+
- Composer

### خطوات التثبيت
```bash
# 1. استنساخ المشروع
git clone [repository-url]
cd TDM-system

# 2. تثبيت المكتبات
composer install

# 3. إعداد البيئة
cp .env.example .env
php artisan key:generate

# 4. إعداد قاعدة البيانات
# تحديث .env بمعلومات قاعدة البيانات

# 5. تشغيل Migrations والـ Seeders
php artisan migrate
php artisan db:seed

# 6. تشغيل الخادم
php artisan serve
```

### بيانات الدخول الافتراضية
```
Admin:     admin / admin123
Keeper:    keeper1 / keeper123
Salesman:  salesman1 / sales123
```

---

## 📝 ملفات التوثيق

### ملفات التطوير (8 ملفات)
1. `1-عملية_طلب_بضاعة_من_المسوق.txt`
2. `2-عملية_إرجاع_بضاعة_من_المسوق.txt`
3. `3-عملية_بيع_بضاعة_للمتجر.txt`
4. `4-عملية_إيصال_القبض_تسديد.txt`
5. `5-عملية_سحب_أرباح_المسوق.txt`
6. `6-عملية_إرجاع_بضاعة_من_المتجر.txt`
7. `7-إدارة_خصومات_الفواتير.txt`
8. `8-إدارة_العروض_الترويجية.txt`

### ملفات إضافية
- `README.md` - نظرة عامة على المشروع
- `database_structure.txt` - هيكل قاعدة البيانات
- `API_ROUTES_CREATED.txt` - قائمة الـ Routes

---

## ✅ قائمة التحقق النهائية

### Backend
- [x] Laravel 10 مثبت ومُعد
- [x] Sanctum للمصادقة
- [x] 26 Migration مكتملة
- [x] 25 Model مكتملة
- [x] 19 API Controller مكتملة
- [x] RESTful API Structure
- [x] Role-based Authorization
- [x] Database Transactions
- [x] Error Handling

### العمليات الرئيسية
- [x] 1. طلب بضاعة من المسوق
- [x] 2. إرجاع بضاعة من المسوق
- [x] 3. بيع بضاعة للمتجر
- [x] 4. إيصال القبض (تسديد)
- [x] 5. سحب أرباح المسوق
- [x] 6. إرجاع بضاعة من المتجر
- [x] 7. إدارة خصومات الفواتير
- [x] 8. إدارة العروض الترويجية

### الميزات الإضافية
- [x] نظام الديون الديناميكي
- [x] نظام العمولات
- [x] العروض الترويجية التلقائية
- [x] خصومات الفواتير التلقائية
- [x] تتبع حركة المخزون
- [x] رفع الصور والمستندات
- [x] سجل التوثيقات

---

## 🎯 الأهداف المحققة

✅ **شفافية كاملة** في جميع العمليات  
✅ **منع النزاعات** بين الأطراف  
✅ **إدارة دقيقة للمخزون** في جميع المراحل  
✅ **نظام ديون مرن** مع تتبع كامل  
✅ **قابل للتوسع** لإضافة ميزات جديدة  
✅ **RESTful API** جاهز للاستخدام  
✅ **توثيق شامل** لكل عملية

---

## 📞 الدعم والصيانة

### للحصول على المساعدة
- مراجعة ملفات التوثيق في مجلد `التطوير_1`
- مراجعة `README.md` للنظرة العامة
- فحص الـ Controllers للأمثلة العملية

### التطوير المستقبلي
- إضافة واجهة مستخدم (Frontend)
- تقارير متقدمة وإحصائيات
- إشعارات في الوقت الفعلي
- تصدير البيانات (PDF, Excel)
- نظام الإشعارات والتنبيهات

---

## 📄 الترخيص

تم تطوير هذا النظام بواسطة فريق Taqnia لإدارة التوزيع والديون

---

**🎉 المشروع مكتمل وجاهز للاستخدام في بيئة الإنتاج**

**تاريخ الإكمال:** 2024  
**الحالة:** ✅ مكتمل 100%
