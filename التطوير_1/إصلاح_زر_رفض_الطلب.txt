================================================================================
                    تعليمات تطبيق إصلاح زر "رفض الطلب"
================================================================================

📋 المشكلة:
-----------
زر "رفض الطلب" في صفحة /warehouse/sales/{id} لا يعمل بسبب:
1. ❌ Route غير موجود في api/warehouse.php
2. ❌ جدول sales_invoice_rejections غير موجود
3. ❌ حالة 'rejected' غير موجودة في enum الحالات

✅ الإصلاحات المطبقة:
---------------------
1. ✅ إضافة Route: PUT /api/warehouse/sales/{id}/reject
2. ✅ إنشاء Migration: 2024_01_01_000026_create_sales_invoice_rejections_table.php
3. ✅ تحديث Migration: إضافة 'rejected' إلى enum الحالات
4. ✅ إنشاء Model: SalesInvoiceRejection.php
5. ✅ تحديث Model: إضافة علاقة rejection() في SalesInvoice

================================================================================
                    خطوات التطبيق (مهم جداً)
================================================================================

⚠️ ملاحظة: يجب تطبيق هذه الخطوات بالترتيب

الخطوة 1: تحديث جدول sales_invoices لإضافة حالة 'rejected'
----------------------------------------------------------------
نظراً لأن الجدول موجود بالفعل، يجب تعديل الـ enum يدوياً:

قم بتشغيل هذا الأمر SQL في قاعدة البيانات:

```sql
ALTER TABLE sales_invoices 
MODIFY COLUMN status ENUM('pending', 'approved', 'cancelled', 'rejected') 
DEFAULT 'pending';
```

الخطوة 2: إنشاء جدول sales_invoice_rejections
------------------------------------------------
قم بتشغيل الأمر التالي:

```bash
php artisan migrate --path=database/migrations/2024_01_01_000026_create_sales_invoice_rejections_table.php
```

الخطوة 3: التحقق من التطبيق
-----------------------------
1. افتح المتصفح واذهب إلى: /warehouse/sales/3
2. تأكد من ظهور زر "رفض الطلب" للفواتير في حالة pending
3. اضغط على الزر وأدخل سبب الرفض
4. تأكد من نجاح العملية

================================================================================
                    البديل: إعادة بناء قاعدة البيانات
================================================================================

إذا كانت قاعدة البيانات في مرحلة التطوير ولا توجد بيانات مهمة:

```bash
php artisan migrate:fresh --seed
```

⚠️ تحذير: هذا الأمر سيحذف جميع البيانات!

================================================================================
                    التحقق من نجاح الإصلاح
================================================================================

✅ تحقق من:
1. وجود Route: GET /api/warehouse/sales/{id}/reject
2. وجود جدول: sales_invoice_rejections
3. حالة 'rejected' في جدول sales_invoices
4. عمل زر "رفض الطلب" بشكل صحيح

================================================================================
                    ملاحظات إضافية
================================================================================

📌 عند رفض الفاتورة:
- ✅ البضاعة تعود من store_pending_stock إلى marketer_actual_stock
- ✅ يتم تسجيل سبب الرفض في جدول sales_invoice_rejections
- ✅ تتغير حالة الفاتورة إلى 'rejected'
- ✅ يتم تسجيل معرف أمين المخزن الذي رفض الفاتورة

📌 الفرق بين Cancelled و Rejected:
- Cancelled: المسوق يلغي الفاتورة بنفسه (قبل التوثيق)
- Rejected: أمين المخزن يرفض الفاتورة (مع تسجيل السبب)

================================================================================
