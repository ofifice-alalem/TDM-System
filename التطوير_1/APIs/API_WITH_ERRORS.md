# 📋 API Endpoints - جميع الحالات (النجاح والأخطاء)

---

## 1️⃣ طلب بضاعة من المسوق

### 🔵 المسوق - إنشاء طلب جديد

**Request:**
```http
POST /api/marketer/requests
Authorization: Bearer {token}
Content-Type: application/json

{
  "items": [
    {"product_id": 1, "quantity": 50}
  ]
}
```

**✅ Success Response (201):**
```json
{
  "message": "تم إنشاء الطلب بنجاح",
  "data": {
    "id": 15,
    "invoice_number": "MR-20240203-0015"
  }
}
```

**❌ Error Responses:**

**401 - غير مصرح:**
```json
{
  "message": "غير مصرح. يرجى تسجيل الدخول أولاً",
  "error": "Unauthenticated"
}
```

**403 - ليس لديك صلاحية:**
```json
{
  "message": "ليس لديك صلاحية للوصول",
  "error": "Forbidden"
}
```

**422 - بيانات غير صحيحة:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "items": ["حقل items مطلوب"],
    "items.0.product_id": ["المنتج غير موجود"],
    "items.0.quantity": ["الكمية يجب أن تكون أكبر من 0"]
  }
}
```

**500 - خطأ في الخادم:**
```json
{
  "message": "فشل إنشاء الطلب",
  "error": "Database connection error"
}
```

---

### 🔵 المسوق - عرض جميع الطلبات

**Request:**
```http
GET /api/marketer/requests
Authorization: Bearer {token}
```

**✅ Success Response (200):**
```json
{
  "message": "قائمة طلبات المسوق",
  "data": [
    {
      "id": 1,
      "invoice_number": "MR-20240203-0001",
      "status": "pending",
      "created_at": "2024-02-03 10:30:00"
    }
  ]
}
```

**✅ Success - لا توجد بيانات (200):**
```json
{
  "message": "قائمة طلبات المسوق",
  "data": []
}
```

**❌ Error - 401:**
```json
{
  "message": "غير مصرح. يرجى تسجيل الدخول أولاً"
}
```

---

### 🔵 المسوق - عرض تفاصيل طلب

**Request:**
```http
GET /api/marketer/requests/5
Authorization: Bearer {token}
```

**✅ Success Response (200):**
```json
{
  "message": "تفاصيل الطلب",
  "data": {
    "request": {
      "id": 5,
      "invoice_number": "MR-20240203-0005",
      "status": "approved",
      "approver_name": "أحمد محمد"
    },
    "items": [
      {
        "product_id": 1,
        "product_name": "منتج 1",
        "quantity": 50
      }
    ]
  }
}
```

**❌ Error - 404 الطلب غير موجود:**
```json
{
  "message": "الطلب غير موجود"
}
```

**❌ Error - 403 ليس طلبك:**
```json
{
  "message": "ليس لديك صلاحية الوصول لهذا الطلب"
}
```

---

### 🔵 المسوق - إلغاء طلب

**Request:**
```http
PUT /api/marketer/requests/5/cancel
Authorization: Bearer {token}
Content-Type: application/json

{
  "notes": "تغيير في الخطة"
}
```

**✅ Success Response (200):**
```json
{
  "message": "تم إلغاء الطلب بنجاح"
}
```

**❌ Error - 404 الطلب غير موجود:**
```json
{
  "message": "الطلب غير موجود أو لا يمكن إلغاؤه"
}
```

**❌ Error - 400 لا يمكن الإلغاء:**
```json
{
  "message": "لا يمكن إلغاء طلب موثق"
}
```

**❌ Error - 400 الحالة غير صحيحة:**
```json
{
  "message": "يمكن إلغاء الطلبات في حالة pending أو approved فقط"
}
```

---

### 🟢 أمين المخزن - الموافقة على طلب

**Request:**
```http
PUT /api/warehouse/requests/5/approve
Authorization: Bearer {token}
```

**✅ Success Response (200):**
```json
{
  "message": "تمت الموافقة على الطلب بنجاح"
}
```

**❌ Error - 404:**
```json
{
  "message": "الطلب غير موجود أو تم معالجته مسبقاً"
}
```

**❌ Error - 400 المخزون غير كافٍ:**
```json
{
  "message": "المخزون غير كافٍ"
}
```

**❌ Error - 400 الحالة غير صحيحة:**
```json
{
  "message": "يمكن الموافقة على الطلبات في حالة pending فقط"
}
```

---

### 🟢 أمين المخزن - رفض طلب

**Request:**
```http
PUT /api/warehouse/requests/5/reject
Authorization: Bearer {token}
Content-Type: application/json

{
  "notes": "المخزون غير متوفر حالياً"
}
```

**✅ Success Response (200):**
```json
{
  "message": "تم رفض الطلب"
}
```

**❌ Error - 422 ملاحظات مطلوبة:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "notes": ["حقل الملاحظات مطلوب عند الرفض"]
  }
}
```

**❌ Error - 404:**
```json
{
  "message": "الطلب غير موجود أو لا يمكن رفضه"
}
```

---

### 🟢 أمين المخزن - توثيق استلام

**Request:**
```http
POST /api/warehouse/requests/5/document
Authorization: Bearer {token}
Content-Type: multipart/form-data

stamped_image: [file]
```

**✅ Success Response (200):**
```json
{
  "message": "تم توثيق استلام البضاعة بنجاح"
}
```

**❌ Error - 422 صورة مطلوبة:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "stamped_image": ["صورة الفاتورة المختومة مطلوبة"]
  }
}
```

**❌ Error - 422 نوع الملف غير صحيح:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "stamped_image": ["يجب أن يكون الملف صورة (jpg, png, pdf)"]
  }
}
```

**❌ Error - 422 حجم الملف كبير:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "stamped_image": ["حجم الملف يجب أن لا يتجاوز 10MB"]
  }
}
```

**❌ Error - 404:**
```json
{
  "message": "الطلب غير موجود أو غير موافق عليه"
}
```

**❌ Error - 400 الحالة غير صحيحة:**
```json
{
  "message": "يمكن توثيق الطلبات في حالة approved فقط"
}
```

---

## 2️⃣ إرجاع بضاعة من المسوق

### 🔵 المسوق - إنشاء طلب إرجاع

**Request:**
```http
POST /api/marketer/returns
Authorization: Bearer {token}
Content-Type: application/json

{
  "items": [
    {"product_id": 1, "quantity": 10}
  ]
}
```

**✅ Success Response (201):**
```json
{
  "message": "تم إنشاء طلب الإرجاع بنجاح",
  "data": {
    "id": 8,
    "invoice_number": "MRR-20240203-0008"
  }
}
```

**❌ Error - 400 الكمية غير متوفرة:**
```json
{
  "message": "الكمية المطلوبة غير متوفرة في مخزونك الفعلي"
}
```

**❌ Error - 422:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "items": ["حقل items مطلوب"],
    "items.0.quantity": ["الكمية يجب أن تكون أكبر من 0"]
  }
}
```

---

## 3️⃣ بيع بضاعة للمتجر

### 🔵 المسوق - إنشاء فاتورة بيع

**Request:**
```http
POST /api/marketer/sales
Authorization: Bearer {token}
Content-Type: application/json

{
  "store_id": 1,
  "items": [
    {"product_id": 1, "quantity": 20}
  ]
}
```

**✅ Success Response (201):**
```json
{
  "message": "تم إنشاء فاتورة البيع بنجاح",
  "data": {
    "id": 25,
    "invoice_number": "SI-20240203-0025"
  }
}
```

**❌ Error - 400 الكمية غير متوفرة:**
```json
{
  "message": "الكمية المطلوبة غير متوفرة في مخزونك"
}
```

**❌ Error - 422 المتجر غير موجود:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "store_id": ["المتجر المحدد غير موجود"]
  }
}
```

**❌ Error - 400 المتجر غير نشط:**
```json
{
  "message": "المتجر المحدد غير نشط حالياً"
}
```

---

### 🔵 المسوق - عرض معلومات رفض الفاتورة

**Request:**
```http
GET /api/marketer/sales/10/rejection
Authorization: Bearer {token}
```

**✅ Success Response (200):**
```json
{
  "message": "معلومات الرفض",
  "data": {
    "sales_invoice_id": 10,
    "reason": "أسعار غير صحيحة",
    "rejected_by": 2,
    "rejected_by_name": "أحمد المخزني",
    "rejected_at": "2024-02-03 14:30:00"
  }
}
```

**❌ Error - 404 لا توجد معلومات رفض:**
```json
{
  "message": "لا توجد معلومات رفض"
}
```

**❌ Error - 404 الفاتورة غير موجودة:**
```json
{
  "message": "الفاتورة غير موجودة"
}
```

---

### 🟢 أمين المخزن - رفض فاتورة

**Request:**
```http
PUT /api/warehouse/sales/10/reject
Authorization: Bearer {token}
Content-Type: application/json

{
  "reason": "أسعار غير صحيحة"
}
```

**✅ Success Response (200):**
```json
{
  "message": "تم رفض الفاتورة بنجاح"
}
```

**❌ Error - 422 السبب مطلوب:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "reason": ["حقل سبب الرفض مطلوب"]
  }
}
```

**❌ Error - 400 الحالة غير صحيحة:**
```json
{
  "message": "يمكن رفض الفواتير في حالة pending فقط"
}
```

---

## 4️⃣ إيصال القبض (تسديد دين المتجر)

### 🔵 المسوق - إنشاء إيصال قبض

**Request:**
```http
POST /api/marketer/payments
Authorization: Bearer {token}
Content-Type: application/json

{
  "store_id": 1,
  "amount": 5000,
  "payment_method": "cash"
}
```

**✅ Success Response (201):**
```json
{
  "message": "تم إنشاء إيصال القبض بنجاح",
  "data": {
    "id": 12,
    "payment_number": "PAY-20240203-0012"
  }
}
```

**❌ Error - 400 المبلغ أكبر من الدين:**
```json
{
  "message": "المبلغ المدخل أكبر من دين المتجر"
}
```

**❌ Error - 400 لا يوجد دين:**
```json
{
  "message": "لا يوجد دين على هذا المتجر"
}
```

**❌ Error - 422 طريقة الدفع غير صحيحة:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "payment_method": ["طريقة الدفع يجب أن تكون: cash, transfer, أو certified_check"]
  }
}
```

**❌ Error - 422 المبلغ غير صحيح:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "amount": ["المبلغ يجب أن يكون أكبر من 0"]
  }
}
```

---

## 5️⃣ سحب أرباح المسوق

### 🔵 المسوق - عرض الرصيد المتاح

**Request:**
```http
GET /api/marketer/withdrawals/balance
Authorization: Bearer {token}
```

**✅ Success Response (200):**
```json
{
  "message": "الرصيد المتاح",
  "data": {
    "total_commissions": 15000,
    "total_withdrawals": 8000,
    "available_balance": 7000
  }
}
```

**✅ Success - لا يوجد رصيد (200):**
```json
{
  "message": "الرصيد المتاح",
  "data": {
    "total_commissions": 0,
    "total_withdrawals": 0,
    "available_balance": 0
  }
}
```

---

### 🔵 المسوق - إنشاء طلب سحب

**Request:**
```http
POST /api/marketer/withdrawals
Authorization: Bearer {token}
Content-Type: application/json

{
  "requested_amount": 5000
}
```

**✅ Success Response (201):**
```json
{
  "message": "تم إنشاء طلب السحب بنجاح",
  "data": {
    "id": 7,
    "requested_amount": 5000
  }
}
```

**❌ Error - 400 المبلغ أكبر من الرصيد:**
```json
{
  "message": "المبلغ المطلوب أكبر من رصيدك المتاح"
}
```

**❌ Error - 400 لا يوجد رصيد:**
```json
{
  "message": "لا يوجد رصيد متاح للسحب"
}
```

**❌ Error - 422:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "requested_amount": ["المبلغ يجب أن يكون أكبر من 0"]
  }
}
```

---

### 🟡 الإدارة - رفض طلب سحب

**Request:**
```http
POST /api/admin/withdrawals/7/reject
Authorization: Bearer {token}
Content-Type: application/json

{
  "notes": "يرجى الانتظار حتى نهاية الشهر"
}
```

**✅ Success Response (200):**
```json
{
  "message": "تم رفض طلب السحب"
}
```

**❌ Error - 422 الملاحظات مطلوبة:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "notes": ["حقل الملاحظات مطلوب عند الرفض"]
  }
}
```

**❌ Error - 400 الحالة غير صحيحة:**
```json
{
  "message": "يمكن رفض الطلبات في حالة pending فقط"
}
```

---

## 6️⃣ إرجاع بضاعة من المتجر

### 🔵 المسوق - إنشاء طلب إرجاع

**Request:**
```http
POST /api/marketer/store-returns
Authorization: Bearer {token}
Content-Type: application/json

{
  "sales_invoice_id": 25,
  "store_id": 1,
  "items": [
    {
      "sales_invoice_item_id": 50,
      "product_id": 1,
      "quantity": 5
    }
  ]
}
```

**✅ Success Response (201):**
```json
{
  "message": "تم إنشاء طلب الإرجاع بنجاح",
  "data": {
    "id": 15,
    "return_number": "SR-20240203-0015"
  }
}
```

**❌ Error - 400 الكمية أكبر من المباعة:**
```json
{
  "message": "الكمية المرجعة أكبر من الكمية المباعة في الفاتورة"
}
```

**❌ Error - 400 الفاتورة غير موثقة:**
```json
{
  "message": "لا يمكن إرجاع بضاعة من فاتورة غير موثقة"
}
```

**❌ Error - 404 الفاتورة غير موجودة:**
```json
{
  "message": "الفاتورة المحددة غير موجودة"
}
```

**❌ Error - 403 ليست فاتورتك:**
```json
{
  "message": "هذه الفاتورة لا تخصك"
}
```

---

## 7️⃣ إدارة خصومات الفواتير

### 🟡 الإدارة - إنشاء خصم

**Request:**
```http
POST /api/admin/discounts
Authorization: Bearer {token}
Content-Type: application/json

{
  "min_amount": 1000,
  "discount_type": "percentage",
  "discount_percentage": 5,
  "start_date": "2024-01-01",
  "end_date": "2024-12-31"
}
```

**✅ Success Response (201):**
```json
{
  "message": "تم إنشاء الخصم بنجاح",
  "data": {
    "id": 5,
    "min_amount": 1000,
    "discount_type": "percentage",
    "discount_percentage": 5
  }
}
```

**❌ Error - 422 نوع الخصم غير صحيح:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "discount_type": ["نوع الخصم يجب أن يكون: percentage أو fixed"]
  }
}
```

**❌ Error - 422 قيمة الخصم مطلوبة:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "discount_percentage": ["نسبة الخصم مطلوبة عند اختيار نوع percentage"]
  }
}
```

**❌ Error - 422 التاريخ غير صحيح:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "end_date": ["تاريخ النهاية يجب أن يكون بعد تاريخ البداية"]
  }
}
```

---

### 🔵 المسوق - عرض الخصومات النشطة

**Request:**
```http
GET /api/marketer/discounts/active
Authorization: Bearer {token}
```

**✅ Success Response (200):**
```json
{
  "message": "الخصومات النشطة",
  "data": [
    {
      "id": 1,
      "min_amount": 1000,
      "discount_type": "percentage",
      "discount_percentage": 5
    }
  ]
}
```

**✅ Success - لا توجد خصومات نشطة (200):**
```json
{
  "message": "الخصومات النشطة",
  "data": []
}
```

---

## 8️⃣ إدارة العروض الترويجية

### 🟡 الإدارة - إنشاء عرض

**Request:**
```http
POST /api/admin/promotions
Authorization: Bearer {token}
Content-Type: application/json

{
  "product_id": 1,
  "min_quantity": 10,
  "free_quantity": 2,
  "start_date": "2024-01-01",
  "end_date": "2024-12-31"
}
```

**✅ Success Response (201):**
```json
{
  "message": "تم إنشاء العرض الترويجي بنجاح",
  "data": {
    "id": 8,
    "product_id": 1,
    "min_quantity": 10,
    "free_quantity": 2
  }
}
```

**❌ Error - 422 المنتج غير موجود:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "product_id": ["المنتج المحدد غير موجود"]
  }
}
```

**❌ Error - 422 الكمية غير صحيحة:**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "min_quantity": ["الكمية الدنيا يجب أن تكون أكبر من 0"],
    "free_quantity": ["الكمية المجانية يجب أن تكون أكبر من 0"]
  }
}
```

**❌ Error - 400 يوجد عرض نشط:**
```json
{
  "message": "يوجد عرض نشط بالفعل لهذا المنتج في نفس الفترة"
}
```

---

## 📊 أخطاء عامة لجميع الـ Endpoints

### 401 - غير مصرح (Unauthenticated)
```json
{
  "message": "غير مصرح. يرجى تسجيل الدخول أولاً",
  "error": "Unauthenticated"
}
```

### 403 - ممنوع (Forbidden)
```json
{
  "message": "ليس لديك صلاحية للوصول",
  "error": "Forbidden"
}
```

### 404 - غير موجود (Not Found)
```json
{
  "message": "المورد المطلوب غير موجود",
  "error": "Not Found"
}
```

### 405 - Method غير مسموح
```json
{
  "message": "الطريقة غير مسموحة",
  "error": "Method Not Allowed"
}
```

### 429 - طلبات كثيرة (Rate Limit)
```json
{
  "message": "عدد كبير من الطلبات. يرجى المحاولة لاحقاً",
  "error": "Too Many Requests"
}
```

### 500 - خطأ في الخادم
```json
{
  "message": "حدث خطأ في الخادم",
  "error": "Internal Server Error"
}
```

### 503 - الخدمة غير متاحة
```json
{
  "message": "الخدمة غير متاحة حالياً",
  "error": "Service Unavailable"
}
```

---

## 🔐 أخطاء المصادقة

### تسجيل الدخول - بيانات خاطئة

**Request:**
```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "wrong_password"
}
```

**❌ Error Response (401):**
```json
{
  "message": "بيانات الدخول غير صحيحة"
}
```

### تسجيل الدخول - حساب معطل

**❌ Error Response (403):**
```json
{
  "message": "حسابك معطل. يرجى التواصل مع الإدارة"
}
```

### تسجيل الدخول - بيانات ناقصة

**❌ Error Response (422):**
```json
{
  "message": "البيانات المدخلة غير صحيحة",
  "errors": {
    "username": ["حقل اسم المستخدم مطلوب"],
    "password": ["حقل كلمة المرور مطلوب"]
  }
}
```

---

**✅ جميع الحالات موثقة (النجاح والأخطاء)**
