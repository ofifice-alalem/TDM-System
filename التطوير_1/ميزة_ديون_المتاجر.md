# ✅ ميزة ديون المتاجر - مكتملة

## 📋 الوصف
صفحة مشتركة بين جميع المستخدمين لعرض المتاجر وديونهم بشكل تفصيلي.

## 🎯 المميزات

### صفحة القائمة الرئيسية (`/stores`)
- ✅ عرض جميع المتاجر في كاردات (مثل صفحة المسوقين)
- ✅ كل كارد يحتوي على:
  - اسم المتجر
  - زر تفعيل/إلغاء تفعيل (أعلى اليسار - صلاحية Admin فقط)
  - إجمالي مبلغ الفواتير
  - إجمالي ما تم تسديده
  - المبلغ المتبقي (بألوان مختلفة: أحمر للديون، أخضر إذا لا يوجد ديون)
  - زر "التفاصيل" للانتقال لصفحة التفاصيل

### صفحة التفاصيل (`/stores/{id}`)
- ✅ معلومات المتجر الكاملة (الاسم، المالك، الهاتف، الموقع)
- ✅ ملخص الحسابات:
  - إجمالي الفواتير
  - إجمالي المدفوع
  - إجمالي المرتجع
  - المتبقي
- ✅ جدول سجل الحركات يعرض:
  - التاريخ
  - نوع العملية (فاتورة بيع / دفعة / إرجاع)
  - رقم المستند
  - اسم المسوق
  - المبلغ (+ للفواتير، - للدفعات والإرجاع)

## 🔐 الصلاحيات

### جميع المستخدمين (Admin, Warehouse Keeper, Salesman)
- ✅ عرض قائمة المتاجر
- ✅ عرض تفاصيل أي متجر
- ✅ عرض سجل الحركات

### Admin فقط
- ✅ تفعيل/إلغاء تفعيل المتاجر

## 📁 الملفات المنشأة

### Controllers
1. **API Controller**
   - `app/Http/Controllers/Api/StoreDebtController.php`
   - Methods:
     - `index()` - عرض جميع المتاجر مع الديون
     - `show($id)` - عرض تفاصيل متجر محدد
     - `toggleActive($id)` - تفعيل/إلغاء تفعيل (Admin فقط)

2. **Web Controller**
   - `app/Http/Controllers/Web/StoreDebtController.php`
   - Methods:
     - `index()` - صفحة القائمة
     - `show($id)` - صفحة التفاصيل

### Views
1. **resources/views/stores/index.blade.php**
   - صفحة عرض جميع المتاجر
   - تصميم كاردات مشابه لصفحة المسوقين
   - زر تفعيل ديناميكي

2. **resources/views/stores/show.blade.php**
   - صفحة تفاصيل المتجر
   - ملخص الحسابات
   - جدول سجل الحركات

### Routes

#### API Routes (`routes/api/common.php`)
```php
Route::get('/stores/debts', [StoreDebtController::class, 'index']);
Route::get('/stores/debts/{id}', [StoreDebtController::class, 'show']);
Route::put('/stores/{id}/toggle-active', [StoreDebtController::class, 'toggleActive'])
    ->middleware('role:admin');
```

#### Web Routes (`routes/web.php`)
```php
Route::get('/stores', [StoreDebtController::class, 'index']);
Route::get('/stores/{id}', [StoreDebtController::class, 'show']);
```

### Sidebar Updates
- ✅ إضافة رابط "ديون المتاجر" لجميع المستخدمين
- ✅ الرابط يظهر في القائمة الجانبية لـ:
  - Admin
  - Warehouse Keeper
  - Salesman

## 🎨 التصميم

### الألوان المستخدمة
- **إجمالي الفواتير:** أزرق (#3b82f6)
- **إجمالي المدفوع:** أخضر (#10b981)
- **إجمالي المرتجع:** برتقالي (#f59e0b)
- **المتبقي (ديون):** أحمر (#ef4444)
- **المتبقي (لا ديون):** أخضر (#10b981)

### المكونات
- كاردات بتصميم عصري مع hover effects
- زر تفعيل/إلغاء تفعيل بتصميم Toggle Switch
- جدول سجل الحركات بتصميم نظيف
- Badges ملونة لأنواع العمليات
- تصميم متجاوب (Responsive)

## 📊 حساب الديون

### المعادلة
```
المتبقي = إجمالي الفواتير - إجمالي المدفوع - إجمالي المرتجع
```

### مصدر البيانات
- جدول `store_debt_ledger` يحتوي على جميع الحركات:
  - `entry_type = 'sale'` → فاتورة بيع (يزيد الدين)
  - `entry_type = 'payment'` → دفعة (يقلل الدين)
  - `entry_type = 'return'` → إرجاع (يقلل الدين)

## 🔗 الروابط

### للوصول للصفحات
- القائمة الرئيسية: `/stores`
- تفاصيل متجر: `/stores/{id}`

### API Endpoints
- `GET /api/stores/debts` - قائمة المتاجر
- `GET /api/stores/debts/{id}` - تفاصيل متجر
- `PUT /api/stores/{id}/toggle-active` - تفعيل/إلغاء تفعيل

## ✅ الحالة
**مكتمل 100%** - جاهز للاستخدام

## 📝 ملاحظات
- زر التفعيل يظهر لجميع المستخدمين لكنه معطل (disabled) لغير الـ Admin
- التصميم متناسق مع باقي صفحات النظام
- يدعم الوضع الليلي (Dark Mode)
- جميع النصوص باللغة العربية
- الأرقام بالتنسيق الإنجليزي مع فواصل الآلاف

## 🎯 الاستخدام

### للمسوق (Salesman)
- يمكنه رؤية ديون جميع المتاجر
- يساعده في معرفة المتاجر التي عليها ديون
- يمكنه متابعة سجل حركاته مع كل متجر

### لأمين المخزن (Warehouse Keeper)
- يمكنه متابعة ديون المتاجر
- يساعده في اتخاذ قرارات الموافقة على الفواتير

### للمدير (Admin)
- يمكنه رؤية جميع الديون
- يمكنه تفعيل/إلغاء تفعيل المتاجر
- يساعده في إدارة المتاجر والديون

---

**تاريخ الإنشاء:** 2025
**الحالة:** ✅ مكتمل وجاهز للإنتاج
